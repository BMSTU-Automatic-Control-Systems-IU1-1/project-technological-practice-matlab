function y = transcendental(x)

%
% Функция нахождения локального минимума функции вида:
%       f(x) = - (2 * sin(x .^ 2) - x);
%   на отрезке 0 <= x <= pi/3
%

y = exp(x) + (1 + exp(2 * x)) ^ (1 / 2) - 2;

end